import { CustomerForm } from '../../../components/customers/CustomerForm';

export default function NewCustomerPage() {
  return <CustomerForm />;
}

export const metadata = {
  title: 'Add New Customer - SmartBooks',
  description: 'Create a new customer record',
};